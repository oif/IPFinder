SET SQL_MODE = "NO_AUTO_VALUE_ON_ZERO";
SET time_zone = "+00:00";

create table ip_0 (
    startAt varchar(30) NOT NULL,
    endAt varchar(30),
    geo varchar(255),
    primary key(startAt)
);

create table ip_1 (
    startAt varchar(30) NOT NULL,
    endAt varchar(30),
    geo varchar(255),
    primary key(startAt)
);

create table ip_2 (
    startAt varchar(30) NOT NULL,
    endAt varchar(30),
    geo varchar(255),
    primary key(startAt)
);

create table ip_3 (
    startAt varchar(30) NOT NULL,
    endAt varchar(30),
    geo varchar(255),
    primary key(startAt)
);

create table ip_4 (
    startAt varchar(30) NOT NULL,
    endAt varchar(30),
    geo varchar(255),
    primary key(startAt)
);

create table ip_5 (
    startAt varchar(30) NOT NULL,
    endAt varchar(30),
    geo varchar(255),
    primary key(startAt)
);